def move_min_to_end(arr):
    min_index = 0
    for i in range(1, len(arr)):
        if arr[i] < arr[min_index]:
            min_index = i
    
    arr.append(arr.pop(min_index))
    return arr

arr = list(map(int, input("Введите 10 чисел через пробел: ").split()))

result = move_min_to_end(arr)
print(result)
