def fill_array_c():
    arr = [[0] * 10 for _ in range(10)]
    for i in range(10):
        for j in range(i + 1, 10):
            arr[i][j] = j - i
        arr[i][i] = 1 if i > 0 else 1
    return arr
